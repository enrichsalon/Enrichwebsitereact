import { colors } from "./colors";
import { createTheme } from '@mui/material/styles';


export default createTheme({
    overrides: {
    },
    palette: {
        primary: {
            main: colors.primary
        }
    },
    typography: {

    },
});
